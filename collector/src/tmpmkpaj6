

extern crate reqwest;
extern crate markov;

use markov::Chain;


fn main() {

	let mut chain = Chain::new();
	match chain.feed_file("../666_ibm.txt"){
		
		Err(e) => println!("{:?}", e),
		_ => ()
	};
	f
		println!("{}", i);
}
