extern crate select;
extern crate reqwest;

use select::document::Document;



fn main() {
    let _resp = reqwest::get("http://textfiles.com/conspiracy/").unwrap();
    let D
    println!("{:?}", Document::from_read(_resp).unwrap());
}
